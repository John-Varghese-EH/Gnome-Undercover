name: Build Debian Package

on:
  push:
    branches: [main]
    paths:
      - 'debian/**'
      - 'scripts/**'
      - 'assets/**'
  pull_request:
    branches: [main]

jobs:
  build-deb:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b  # v4.1.4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y devscripts build-essential debhelper dh-python python3-all python3-setuptools

      - name: Build Debian package
        run: |
          debuild -us -uc -b

      - name: Upload .deb artifact
        uses: actions/upload-artifact@0b7f8339f67a2727ae38307212479e00b213b632  # v4.0.0
        with:
          name: gnome-undercover-deb
          path: ../gnome-undercover_*.deb
          retention-days: 7

      - name: Test package installation
        run: |
          sudo dpkg -i ../gnome-undercover_*.deb || true
          sudo apt-get install -f -y
          which gnome-undercover
          echo "✅ Package installs successfully!"
